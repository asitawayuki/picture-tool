[package]
name = "picture-tool"
version = "0.1.0"
edition = "2021"
license.workspace = true

[dependencies]
# `clap` feature で core の ConversionMode / BackgroundColor をそのまま
# コマンドライン引数として受け取る（CLI 側で enum を複製しない / S6-CLI-3）。
picture-tool-core = { path = "../core", features = ["clap"] }
clap.workspace = true
anyhow.workspace = true
serde_json.workspace = true
